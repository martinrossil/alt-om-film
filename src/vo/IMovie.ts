export default interface IMovie {
    genrer: string, // genre_ids
    id: number, // id
    // p: string // poster_path
    premiere: string, // release_date
    title: string, // title
    rating: number // vote_average
}
